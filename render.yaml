services:
  - type: web
    name: foresight-scoring-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn service.api:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
